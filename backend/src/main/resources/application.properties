spring.datasource.url=jdbc:postgresql://localhost:5432/smartcoach
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=smartcoach
spring.datasource.password=smartcoach123
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# File upload directory (relative to backend working directory)
file.upload-dir=uploads

# Allow slf4j to print startup logs
logging.level.org.springframework=INFO
